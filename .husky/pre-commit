#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

# Run type checking on staged files
echo "📝 Type checking..."
npm run typecheck 2>/dev/null || echo "⚠️ Type check failed, but continuing..."

echo "✅ Pre-commit checks passed!"
